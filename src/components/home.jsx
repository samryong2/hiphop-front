import React from 'react';
import BigBanner from './banner/big_banner/big_banner';

const Home = (props) => {
    return(
        <section>
            <BigBanner/>
            
        </section>
    );
};

export default Home;